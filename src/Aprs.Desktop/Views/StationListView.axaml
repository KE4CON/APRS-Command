<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Aprs.Desktop.ViewModels"
             x:Class="Aprs.Desktop.Views.StationListView"
             x:DataType="vm:StationListViewModel">
  <Grid RowDefinitions="Auto,Auto,*"
        RowSpacing="8">
    <Grid ColumnDefinitions="*,Auto"
          ColumnSpacing="8">
      <StackPanel Spacing="1">
        <TextBlock Text="Station List"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimaryBrush}" />
        <TextBlock Text="{Binding RowCount, StringFormat='{}{0} visible stations'}"
                   FontSize="12"
                   Foreground="{DynamicResource TextSubtleBrush}" />
      </StackPanel>
      <Button Grid.Column="1"
              Padding="8,4"
              Click="SortByLastHeard_Click">
        <TextBlock Text="Recent" />
      </Button>
    </Grid>

    <Grid Grid.Row="1"
          RowDefinitions="Auto,Auto,Auto"
          RowSpacing="6">
      <TextBox Watermark="Search callsign or label"
               Text="{Binding SearchText}"
               TextChanged="SearchTextBox_TextChanged" />

      <Grid Grid.Row="1"
            ColumnDefinitions="*,*"
            ColumnSpacing="6">
        <CheckBox Content="Active only"
                  IsChecked="{Binding ShowActiveOnly}"
                  Checked="ActiveOnlyCheckBox_Changed"
                  Unchecked="ActiveOnlyCheckBox_Changed" />
        <CheckBox Grid.Column="1"
                  Content="Show expired"
                  IsChecked="{Binding ShowExpiredStations}"
                  Checked="ShowExpiredCheckBox_Changed"
                  Unchecked="ShowExpiredCheckBox_Changed" />
      </Grid>

      <ComboBox Grid.Row="2"
                SelectedItem="{Binding SelectedPacketSourceFilter}"
                SelectionChanged="PacketSourceComboBox_SelectionChanged"
                ItemsSource="{Binding PacketSourceFilterOptions}" />
    </Grid>

    <Border Grid.Row="2"
            BorderBrush="{DynamicResource TextSidebarBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Background="{DynamicResource TextTitleBarBrush}"
            ClipToBounds="True">
      <Grid RowDefinitions="Auto,*">
        <Border Background="{DynamicResource TextOnDarkBrush}"
                Padding="8,6">
          <Grid ColumnDefinitions="1.2*,1.2*,1.4*,.9*,.9*"
                ColumnSpacing="6">
            <Button Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="SortByCallsign_Click">
              <TextBlock Text="Call"
                         FontWeight="SemiBold"
                         Foreground="{DynamicResource TitleBarBorderBrush}" />
            </Button>
            <Button Grid.Column="1"
                    Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="SortByDisplayName_Click">
              <TextBlock Text="Name"
                         FontWeight="SemiBold"
                         Foreground="{DynamicResource TitleBarBorderBrush}" />
            </Button>
            <TextBlock Grid.Column="2"
                       Text="Symbol"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TitleBarBorderBrush}" />
            <Button Grid.Column="3"
                    Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="SortByAgeState_Click">
              <TextBlock Text="Age"
                         FontWeight="SemiBold"
                         Foreground="{DynamicResource TitleBarBorderBrush}" />
            </Button>
            <Button Grid.Column="4"
                    Padding="0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="SortByPacketSource_Click">
              <TextBlock Text="Src"
                         FontWeight="SemiBold"
                         Foreground="{DynamicResource TitleBarBorderBrush}" />
            </Button>
          </Grid>
        </Border>

        <ListBox x:Name="StationRowsListBox"
                 Grid.Row="1"
                 ItemsSource="{Binding Rows}"
                 SelectionChanged="StationRowsListBox_SelectionChanged"
                 Background="{DynamicResource TextTitleBarBrush}"
                 BorderThickness="0">
          <ListBox.ItemTemplate>
            <DataTemplate DataType="vm:StationListRowViewModel">
              <Border Padding="8,7"
                      BorderBrush="{DynamicResource TextOnDarkBrush}"
                      BorderThickness="0,0,0,1">
                <Grid RowDefinitions="Auto,Auto,Auto"
                      RowSpacing="3">
                  <Grid ColumnDefinitions="1.2*,1.2*,1.4*,.9*,.9*"
                        ColumnSpacing="6">
                    <StackPanel Orientation="Horizontal" Spacing="4" VerticalAlignment="Center">
                      <TextBlock Text="{Binding Callsign}"
                                 FontWeight="SemiBold"
                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                 TextTrimming="CharacterEllipsis" />
                      <Border IsVisible="{Binding IsLoRa}"
                              Background="#7c3aed" CornerRadius="2"
                              Padding="3,1" VerticalAlignment="Center">
                        <TextBlock Text="LoRa" FontSize="8" FontWeight="Bold"
                                   Foreground="White" />
                      </Border>
                    </StackPanel>
                    <TextBlock Grid.Column="1"
                               Text="{Binding DisplayName}"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="2"
                               Text="{Binding SymbolDescription}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="3"
                               Text="{Binding AgeStateLabel}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="4"
                               Text="{Binding PacketSourceLabel}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextTrimming="CharacterEllipsis" />
                  </Grid>
                  <Grid Grid.Row="1"
                        ColumnDefinitions="*,*,*,*"
                        ColumnSpacing="6">
                    <TextBlock Text="{Binding LastHeard}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11" />
                    <TextBlock Grid.Column="1"
                               Text="{Binding Speed}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11" />
                    <TextBlock Grid.Column="2"
                               Text="{Binding Course}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11" />
                    <TextBlock Grid.Column="3"
                               Text="{Binding Distance}"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11" />
                  </Grid>
                  <TextBlock Grid.Row="2"
                             Text="{Binding Comment}"
                             Foreground="{DynamicResource TextSecondaryBrush}"
                             FontSize="11"
                             TextTrimming="CharacterEllipsis" />
                </Grid>
              </Border>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Grid>
    </Border>
  </Grid>
</UserControl>
